source 'https://rubygems.org'

gem 'diff-lcs', '~> 1.2.1'

# mailcatcher suggest to install in global gemset so install it manually
#
# group :development, :test do
#   # Run mailcatcher, set app to deliver to smtp://127.0.0.1:1025
#   # instead of your default SMTP server, then check out mails at http://127.0.0.1:1080
#   gem 'mailcatcher'
# end

