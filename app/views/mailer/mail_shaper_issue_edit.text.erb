<%= l(:text_issue_updated, :id => "##{@issue.id}", :author => @journal.user) %>
<% journal_details = (@can_view_time_entries ? @journal.details : @journal.details.reject{|k| k.property == 'time_entry'})  %>
<% details_to_strings(journal_details, true).each do |string| -%>
<%= string %>
<% end -%>

<% if @journal.notes? -%>
<%= @journal.notes %>

<% end -%>
----------------------------------------
<%= render :partial => 'issue', :formats => [:text], :locals => { :issue => @issue, :issue_url => @issue_url, :can_view_time_entries => @can_view_time_entries } %>
